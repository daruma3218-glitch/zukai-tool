# 図解つくーる：ディレクター向け改善の設計（2026-09-24）

設計・実装：Claude（Opus 5.5）。本番へ出す前に、戻せる形で段階的に確かめる。
根拠：ディレクターへの聞き取り（時間がかかるのは「図解の直し」「写真探し」「地図チェック・修正」。いちばんの要望は「候補を並べて選ぶ」。追加の要望は「文字を全部消す」「左右反転」）。

## 方針

- **今の使い方・URL・出力を壊さない。** 追加機能は選んだ時だけ動く。既定の変更は画像モデル（下記）だけ。
- **元の画像は上書きしない。** 直した画像は別ファイル（版）として並べ、どれを使うかは「採用」で記録する。
- **費用が増える操作は、始める前に枚数を表示する。** 地図は画像AIを使わずに描くので、画像代はかからない。
- **戻れるように。** 作業は `claude/zukai-director-tools-20260924` ブランチ（基点は本番 `b009e06`）。本番mainと移行用ブランチには触れない。設定（環境変数）で機能ごとに止められる。

## 機能

### 0. 標準の画像モデルを GPT Image 2.5 Flare に

- 生成の既定を `gpt-image-2` → `gpt-image-2.5-flare` に変更（OpenAI公式：gpt-image-2より高品質・待ち時間は約半分・トークン単価は同じ）。画面の既定のプロバイダもGPTにする（ディレクターの手順でも生成モデルは「GPT」が基本）。
- 画像の手直し（下記2）は、編集の精度を重視した `gpt-image-2.5-sunburst` を使う。
- 画面で選んだ値が優先。環境変数 `OPENAI_IMAGE_MODEL` / `ZUKAI_EDIT_MODEL` で戻せる。

### 1. 生成物を30日残し、古いものから自動で消す（`retention.py`）

- 保存先は既存の仕組みどおり `DATA_DIR`→`/data`（Renderのディスク）。ディスクを付けると再起動・更新で消えなくなる。
- 完了またはエラーで終わったジョブを、最後の更新から30日で削除（`ZUKAI_RETENTION_DAYS`、0で無効）。
- ディスク使用量が80%を超えたら、完了済みの古いジョブから70%まで削除（`ZUKAI_RETENTION_MAX_USAGE`）。
- 実行中・待機中のジョブは消さない（3日以上更新がない「止まったジョブ」だけは対象）。ジョブIDの形（`YYYYMMDD_HHMMSS`）のフォルダ以外は消さない。削除は `retention_log.jsonl` に記録。
- 起動時と6時間ごと、ジョブ完了時に実行。複数ワーカーでも同時に走らないようロックファイルを使う。
- 進捗画面に「このジョブは○月○日に自動削除されます」を表示する。

### 2. 1枚ずつの手直し（`image_edit.py`）

- 拡大表示から「左右反転」「文字を全部消す」「指示して直す（自由記述）」を選べる。
- 左右反転はAIを使わずその場で作る（費用なし）。
- 文字消し・指示直しはOpenAIの画像編集（Sunburst）。16:9の元画像を編集用の比率に余白で広げて送り、結果を元の範囲に切り戻して、構図がずれないようにする。1回あたり画像1枚分の費用。
- 直した画像は `diagram_012__e1.png` のような別ファイル。元画像は残る。記録は `edits.json`。

### 3. 候補を並べて選ぶ（`prompter.py` / `pipeline.py` / 画面）

- 作成画面に「候補の出し方」を追加：
  - 1案（従来どおり）
  - 2案（同じ型で構図違い）…「章ごとに2回生成して選ぶ」を1回で済ませる。画像代は今の2回生成と同じ
  - 型違い（図解・イメージ画像・実写風・地図から、その文に合う2〜3案）
- 生成前に「○箇所 × ○案 = ○枚」を表示する。
- 結果画面は、同じ抜粋の候補を横に並べる。各候補に「採用」ボタン。採用は `adoption.json` に記録し、「採用分だけZIP」で取り出せる（抜粋・章・ファイル名の対応表CSV付き）。従来の「ZIPでダウンロード」も残す。
- ファイル名は1案なら従来どおり `diagram_004.png`、複数案なら `diagram_004_a.png` / `_b` / `_c`。各画像は一意の `index`、抜粋ごとの `group`、案の `variant` を持つ。

### 4. 地図は実際の地図データで描く（`map_renderer.py`）

- ディレクターの指摘（黒海の形、カスピ海が2つ、日本が2つ）への対策。種類が「地図」の箇所は画像AIを使わず、Natural Earth（公有）の国境データから描く。国境は公式の「日本の立場版」（北方領土・竹島・尖閣は日本、クリミアはウクライナ）。
- AI（ASTRA）は「どの国を映すか・どの国を何色で強調するか・ピン・矢印」だけを決める（`map_spec`）。国の形・位置はデータから描くので誤らない。
- ピンの座標はAIが出すため、「その国の範囲内（沿岸は少し外側まで許容）にあるか」を地図データで確認し、合わないピンは載せずにログに残す。
- 色はチャンネルの配色（背景 #D9E1E8、メイン #1B365D、比較 #2C4C3B、中立 #E5A91A、警告 #A6192E、注目 #B7950B）。国名は日本語（「中国」「韓国」「北朝鮮」など動画向けの短い名前）。「文字なし」を選んだ時はラベルを描かない。
- 仕様を読めない時は、抜粋に出てくる国名から強調する国を決めて描く（必ず何かしらの正しい地図が出る）。
- 歴史の回でも国境は現代のもの。歴史の国境は対象外（必要なら国名のラベルだけ抜粋の語に置き換える）。
- 作成画面で「地図の作り方：実データ（推奨）／AI画像（従来）」を選べる。`ZUKAI_MAP_RENDERER=off` で従来に戻せる。
- 同梱データ：`geodata/countries.json`（Natural Earth 1:10m・日本の立場版を0.02度で間引き、公有）、`assets/fonts/NotoSansJP-Bold.ttf`（SIL OFL 1.1、`assets/fonts/OFL.txt`）。

### 5. 権利のはっきりした写真の候補（次の段階）

- 実写が合う箇所で、Wikimedia Commons などライセンスが確認できる写真を検索し、候補として並べる。出典・作者・ライセンスを記録。今回は設計のみ。

## 変更するファイル

| ファイル | 内容 |
|---|---|
| `retention.py`（新規） | 自動削除 |
| `image_edit.py`（新規） | 左右反転・文字消し・指示直し、版と採用の記録 |
| `map_renderer.py`（新規） | 地図の仕様チェックと描画 |
| `director_routes.py`（新規） | 手直し・採用・採用分ZIP・保存期限のAPI（Blueprint） |
| `generator.py` | 既定モデル、1枚ごとのファイル名、地図の描画、画像編集の呼び出し |
| `prompter.py` | 候補の出し分け、地図の仕様の出力 |
| `pipeline.py` | 候補の展開、manifestへの記録 |
| `app.py` | 新しい入力欄、Blueprint登録、自動削除の起動、候補ファイル名の読み取り |
| `templates/upload.html` | 候補の出し方・地図の作り方・既定値、前回の選択を覚える |
| `templates/progress.html` | 候補の並び、採用、手直しボタン、版の一覧、保存期限 |

## 確認と切り替え

- 自動テスト：既存（Python 12件・JS 4件）を維持し、機能ごとの検知・見逃しのテストを追加。
- 画面確認：9/23バックアップの図解データをローカルで開いて表示を確認（費用なし）。
- 実生成の確認は最小限（数枚＋文字消し1回）。費用は事前に伝える。
- 本番への反映は、10GBディスク付きの新しいサービスへの移行に合わせて行う。今の本番はディスクがなく、更新すると保存中の画像が消えるため、先に出さない。
- 新しいサービスの設定：`DATA_DIR=/data`（ディスクのマウント先）、`OPENAI_IMAGE_MODEL=gpt-image-2.5-flare`（旧値 `gpt-image-2` のままだと画面で選ばない時の既定が従来モデルになる）、必要に応じて `ZUKAI_RETENTION_DAYS` / `ZUKAI_RETENTION_MAX_USAGE` / `ZUKAI_EDIT_MODEL` / `ZUKAI_MAP_RENDERER`。`render.yaml` は既存サービスの設定同期に使われるため、この段階では変更しない。
- 戻し方：本番は `b009e06` のまま。反映後に戻す場合はRenderの以前のデプロイへ戻す。新しいファイル（`adoption.json` など）は旧版では無視されるだけで、データは壊れない。
