# サブスクリプションCLI運用

2026-09-06: 文章生成・分類・検索・検証・OCRは、Claude CodeまたはCodexの公式CLIで実行する。有料LLM APIへの自動切替と、旧設定によるAPI経路の再有効化は廃止。

- 通常はClaude。Codexを指定した既存処理はCodexから開始する。
- 一時障害・認証失敗・利用上限は同じCLIで最大3回（5秒、15秒待機）、次に他方で最大3回。未インストール等は無意味な反復を省く。
- 両方で継続できなければ停止し、再開用入力と試行履歴を保存する。通知先はChatwork「Claudeアシスタント」433846285。プロンプトや認証情報は通知しない。
- 通知はジョブ単位で重複を抑止し、通信失敗時は別途再送する。通知失敗を理由に本文を再生成しない。
- Windowsの共通状態保存先はsubsk-worker/state。Web版は既存Supabaseゲートウェイ経由。ワーカー不在時は有料APIを使わず、認証付きの通知中継へ停止を知らせる。
- Chatwork対話エージェントのSDKツールセッションは、編集・投稿の二重実行を避けて安全に停止する。状態を別CLIへ推測で移植しない。
- 画像そのものの生成、音声合成、YouTube/Trello等の外部サービスAPIは別機能。今回のテキストLLM切替対象ではない。

CLIは必ずサブスク認証を確認し、子プロセスから有料APIキー・外部プロバイダー設定を除去する。Claudeの追加使用クレジットも無効化を確認した。アカウント側で追加クレジットを有効化するとCLIでも追加利用が可能になるため、サブスク認証だけから追加料金ゼロとは判定しない。

共通実装の正本は I:/AI_Workspace/subsk-worker/subscription_runtime.py。各配備フォルダーへ同一ファイルを同梱する。sync_subscription_runtime.py --check で差異を検出する。状態・入力・通知DBはGitへ含めない。
